// import { downloadBrowser } from '@puppeteer/browsers';
// import path from 'path';
// import os from 'os';

// (async () => {
//   const installPath = path.join(os.tmpdir(), 'clipforge-chrome-shell');

//   const installed = await downloadBrowser({
//     browser: 'chrome',
//     buildId: 'latest',
//     cacheDir: installPath,
//   });

//   console.log('✅ Chrome Headless Shell installé dans :', installed.executablePath);
// })();
